{
  "name": "bbdc4-hedge-monitor",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cd web && npm run dev",
    "build": "cd web && npm install && npm run build",
    "start": "cd web && npm run start"
  },
  "dependencies": {
    "next": "16.2.9"
  }
}
